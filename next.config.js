/** @type {import('next').NextConfig} */
const nextConfig = {
    // Enabling React Strict Mode (optional, but good for development)
    reactStrictMode: true,

    // Enabling SWC Minification
    swcMinify: true,
};

module.exports = nextConfig;
